# Assets

Folder ini berisi aset build-time seperti gambar, font, yang diimpor di kode.

## Kegunaan

- Optimasi aset (Astro proses otomatis).
- Import di komponen: `import myImage from '../assets/image.png';`

## Contoh File

- `logo.svg`: Logo situs.
  Gunakan: `<img src={logo} alt="Logo" />`
